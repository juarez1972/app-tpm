# Terraform state may contain sensitive material (device seeds, connector tokens).
# Never commit state or local provider caches.
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
